"""
Setup configuration for FICCT SpringCode Backend.
This file is mainly used to establish Python package structure.
"""

from setuptools import setup, find_packages

setup(
    name="ficct_springcode_backend",
    version="0.1",
    packages=find_packages(),
)
