# {{ project_name }}

{{ description }}

## 🚀 Getting Started

This is a SpringBoot application generated from UML diagrams using the FICCT Spring Code platform.

### 📋 Prerequisites

- **Java {{ java_version }}** or higher
- **Maven 3.6+** or higher
- **{{ database_name }}** database (configured in application.properties)

### 🛠️ Installation

1. **Clone the repository**
```bash
git clone <repository-url>
cd {{ artifact_id }}
```

2. **Configure database connection**
   
   Edit `src/main/resources/application.properties` and update the database configuration:
   ```properties
   spring.datasource.url=jdbc:postgresql://localhost:5432/your_database
   spring.datasource.username=your_username
   spring.datasource.password=your_password
   ```

3. **Build the project**
```bash
mvn clean install
```

4. **Run the application**
```bash
mvn spring-boot:run
```

The application will start on **http://localhost:{{ server_port }}**

## 📚 API Documentation

Once the application is running, you can access the API documentation at:

- **Swagger UI**: http://localhost:{{ server_port }}/swagger-ui.html
- **OpenAPI JSON**: http://localhost:{{ server_port }}/api-docs

## 🏗️ Project Structure

```
src/
├── main/java/{{ package_path }}/
│   ├── {{ application_name }}.java     # Main application class
│   ├── entities/                       # JPA Entity classes
│   ├── repositories/                   # JPA Repository interfaces  
│   ├── services/                       # Business logic services
│   ├── controllers/                    # REST API controllers
│   └── dto/                           # Data Transfer Objects
└── main/resources/
    ├── application.properties          # Application configuration
    └── banner.txt                     # Custom startup banner
```

## 🎯 Generated Entities

{% for entity in entities %}
### {{ entity.name }}
{{ entity.description }}

**Endpoints:**
- `GET /api/v1/{{ entity.name | kebab_case }}` - Get all {{ entity.name }}s
- `GET /api/v1/{{ entity.name | kebab_case }}/{id}` - Get {{ entity.name }} by ID
- `POST /api/v1/{{ entity.name | kebab_case }}` - Create new {{ entity.name }}
- `PUT /api/v1/{{ entity.name | kebab_case }}/{id}` - Update {{ entity.name }}
- `DELETE /api/v1/{{ entity.name | kebab_case }}/{id}` - Delete {{ entity.name }}

{% endfor %}

## 🔧 Configuration

### Database Profiles

The application supports multiple database configurations:

- **H2 (Development)**: In-memory database for testing
- **PostgreSQL (Production)**: Production-ready database

### Environment Profiles

- **dev**: Development profile with debug logging
- **prod**: Production profile with optimized settings

To run with a specific profile:
```bash
mvn spring-boot:run -Dspring-boot.run.profiles=prod
```

## 🧪 Testing

Run the test suite:
```bash
mvn test
```

Run integration tests:
```bash
mvn verify
```

## 📦 Building for Production

Create a production-ready JAR:
```bash
mvn clean package -Pprod
```

The JAR file will be created in the `target/` directory.

## 🐳 Docker Support

Build Docker image:
```bash
docker build -t {{ artifact_id }}:{{ version }} .
```

Run with Docker:
```bash
docker run -p {{ server_port }}:{{ server_port }} {{ artifact_id }}:{{ version }}
```

## 🔍 Monitoring

The application includes Spring Boot Actuator for monitoring:

- **Health Check**: http://localhost:{{ server_port }}/actuator/health
- **Metrics**: http://localhost:{{ server_port }}/actuator/metrics
- **Application Info**: http://localhost:{{ server_port }}/actuator/info

## 🤝 Contributing

This project was generated from UML diagrams. To make changes:

1. Update the UML diagrams in the FICCT Spring Code platform
2. Regenerate the code
3. Test your changes
4. Deploy

## 📄 License

This project is licensed under the MIT License - see the LICENSE file for details.

## 🆘 Support

For support and questions:

- **Documentation**: Check the API documentation at `/swagger-ui.html`
- **Issues**: Report issues in the project repository
- **Community**: Join our developer community

---

**Generated by FICCT Spring Code Platform**  
*Transforming UML diagrams into production-ready SpringBoot applications*
